@startuml
start

:Import ride CSV data;
:Map GPS traces to OSM segments;
note right
Use Valhalla
end note
:Assign each record to time window\n(TrafficTime, Weekday, Year);

:Aggregate events per OSM segment;
:Compute SRS per segment;

:Compute RRS per administrative region;

:Compute RRS per SimRa region;

stop
@enduml
